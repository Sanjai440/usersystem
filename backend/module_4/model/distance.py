# import torch

# def euclidean_distance(v1, v2):
#     return torch.sqrt(torch.sum((v1 - v2) ** 2))













import torch

def euclidean_distance(v1, v2):
    return torch.sqrt(torch.sum((v1 - v2) ** 2))